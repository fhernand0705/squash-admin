// assign object to object id (to locate it) that you want to delete
// access object within html element (using the dom)
// remove (deleted) object with styling (set display to none)


$('#cohort-<%= @cohort.id %>').fadeOut();
